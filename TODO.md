# TODO — 향후 작업 / 미결 사항

풍산 여행 웹앱의 추후 작업을 누적 관리한다. 새 미결 사항이 생기면 여기에 추가한다.

## 역할(스파이) 시스템 후속

- [ ] **2일차 밤 전체 정체 공개 연출**
  모두의 정체 + 팀 토큰 총합 + 승리팀을 극적으로 공개하는 마무리.
  방식 미정 — 관리자 버튼 1개 vs 팀 배정식 같은 전체화면 결산 연출 중 택1.
  구현 토대는 이미 준비됨: `player_roles.revealed`를 일괄 `true`로 바꾸면
  RLS상 전원이 서로의 역할을 읽게 된다 (`supabase/migrations/0010_roles.sql`).

- [ ] **개인 역할 카드 확인 UX 재설계**
  역할이 2종(member/spy) 이상으로 늘면 `components/MyRoleCard.tsx`의 문구·표시
  방식을 다시 다듬는다. (현재는 스파이/충성 2종 기준의 잠정 설계.)

- [ ] **추가 역할 (타짜·금고지기·외교관 등)**
  `player_roles.role` 체크 제약에 값 추가 + `assign_roles` RPC 배정 로직 확장
  (`supabase/migrations/0010_roles.sql`). 별도 설계 라운드에서 더 재밌게 기획.

- [ ] **배정식 시작 조건 "10명 전원 접속" 게이트**
  현재는 관리자가 현장에서 다 모이면 수동으로 배정식을 시작한다.
  온라인 프레즌스 기반 강제 게이트는 별도 기능 — 필요 시 후속.
